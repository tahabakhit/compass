interface:
  display_name: "Do"
  short_description: "Use when unified router that auto-routes user intent to the right orchestrator or skill. Classifies input by scope, complexity, persistence needs, and parallelism, then..."
policy:
  allow_implicit_invocation: false

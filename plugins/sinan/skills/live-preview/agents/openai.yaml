interface:
  display_name: "Live Preview"
  short_description: "Use when mid-build visual verification loop. Takes screenshots of components during construction, not just after. Catches visual regressions and invisible features before they..."
policy:
  allow_implicit_invocation: false

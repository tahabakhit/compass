interface:
  display_name: "Cost"
  short_description: "Use when deep cost exploration and transparency. Shows real token usage, session costs, campaign spend, burn rates, and model breakdown. Reads Claude Code's native session data..."
policy:
  allow_implicit_invocation: false

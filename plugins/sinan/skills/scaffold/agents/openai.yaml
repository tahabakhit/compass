interface:
  display_name: "Scaffold"
  short_description: "Use when project-aware file generation. Reads existing codebase conventions (naming, structure, imports, exports, test patterns) then generates new files that match exactly...."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Scaffold"
  short_description: "Create agent guidance surfaces"
  default_prompt: "Use $scaffold to set up agent instructions for this repo."
policy:
  allow_implicit_invocation: false
routing:
  triggers:
    - scaffold
    - agent scaffold
    - scaffold instructions
    - agent instructions
    - setup workspace
    - set up instructions
    - greenfield repo
    - agents.md
    - claude.md
    - glossary.md
    - github labels
    - github workflows
  intents:
    - setup
  task_sizes:
    - full
    - workflow
  allow_implicit_invocation: false

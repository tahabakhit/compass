interface:
  display_name: "TDD"
  short_description: "Build changes through red-green-refactor"
  default_prompt: "Use $tdd to implement this behavior with a focused failing test first."
policy:
  allow_implicit_invocation: false
routing:
  triggers:
    - tdd
    - test driven
    - red green refactor
    - implement with tests
  intents:
    - implement
  task_sizes:
    - full
    - workflow
  allow_implicit_invocation: false

@AGENTS.md

# Claude Entrypoint

- Use Claude plan mode for broad scaffold, architecture, and implementation work.
- Use the installed Sinan plugin skills for routing. When deterministic checks are needed, run the packaged Sinan CLI from the installed plugin context instead of importing repo-local Python modules.
- Keep only Claude-specific tool, command, and permission notes here; shared project policy belongs in `AGENTS.md` and `.agents/`.

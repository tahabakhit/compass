# Claude Surface

- Start from `CLAUDE.md`, which imports `.agents/README.md`.
- Keep Claude-specific command or permission notes here.
- Shared project policy belongs in `.agents/`, not duplicated in `CLAUDE.md`.

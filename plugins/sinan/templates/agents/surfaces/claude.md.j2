# Claude Surface

- Start from `CLAUDE.md`, which imports `AGENTS.md`.
- Keep Claude-specific command, tool, or permission notes in `CLAUDE.md` after the `@AGENTS.md` import.
- Shared project policy belongs in `AGENTS.md` and `.agents/`, not duplicated in Claude-only notes.

# Codex Surface

- Start from `AGENTS.md`, then read this `.agents/` bundle when the task needs repo policy.
- Use Codex plan mode for bootstrap, scaffold, architecture, and broad implementation work.
- Keep implementation state in `.workflow-state/` only when generated by a Sinan command.
